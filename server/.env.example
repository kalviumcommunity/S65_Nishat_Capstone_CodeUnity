# Database Configuration
# MongoDB connection string (required)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority

# Redis Cache Configuration (optional)
# Leave empty to run without Redis


# JWT Authentication (required)
JWT_SECRET=your_secure_random_jwt_secret_key_here

# Google AI API (Gemini) (required for AI features)
# Get your API key from: https://aistudio.google.com/app/apikey
GOOGLE_AI_API_KEY=your_google_ai_api_key_here

# OAuth Configuration (optional for 3rd party authentication)
# Google OAuth 2.0: https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
GOOGLE_CALLBACK_URL=http://localhost:8080/api/auth/google/callback

# GitHub OAuth: https://github.com/settings/developers
GITHUB_CLIENT_ID=your_github_client_id_here
GITHUB_CLIENT_SECRET=your_github_client_secret_here
GITHUB_CALLBACK_URL=http://localhost:8080/api/auth/github/callback

# Frontend URL (for OAuth redirects after authentication)
CLIENT_URL=http://localhost:5173

# Email Service Configuration (required for OTP and notifications)
# Use Gmail with App Password
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_gmail_app_password

# Server Configuration
PORT=8080
NODE_ENV=development

# Instructions:
# 1. Copy this file to .env
# 2. Update all values marked as 'your_' with actual credentials
# 3. For Gmail App Password: https://support.google.com/accounts/answer/185833
# 4. Keep .env file secure and never commit it to version control
